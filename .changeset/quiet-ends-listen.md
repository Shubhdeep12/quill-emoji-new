---
"quill-emoji-new": minor
---

- add `registerAliases(aliases)` to register legacy/custom shortcode aliases without overriding existing shortcodes
- add `legacyAliases` option to `registerQuillEmojiV2(...)`
- export `registerAliases` from the main entrypoint
